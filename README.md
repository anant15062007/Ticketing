("If issue is resolved Give syntax to close the issue")
If provider comments the sender should get notified.
The original email must be written as is.
Look for other keywords like close.

Web interface:-
Admin etc
Priority